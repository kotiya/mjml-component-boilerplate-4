import React from "react";
import { Text } from "@react-email/components";

const AccordionText = ({ children }) => {
  return <Text lineHeight="20px">{children}</Text>;
};

export default AccordionText;
